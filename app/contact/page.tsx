import Heading from "@/components/ui/Heading";
import Tagline from "@/components/ui/Tagline";

export default function ContactPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 bg-cream px-6 pt-16 pb-24 text-center">
      <Heading>let&rsquo;s talk! or we can just send carrier pigeons.</Heading>
      <Tagline>the bulletin board lands here in a later pass.</Tagline>
    </main>
  );
}
